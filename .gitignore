# 查看当前远程源（确认只有你自己的仓库）
# git remote -v
# 添加仓库地址
# git remote add upstream https://github.com/username/repository.git
# 如何更新 (拉取上游master分支代码)
# git pull upstream master
# 合并代码更新
#  git merge upstream/master --allow-unrelated-histories
# 注意/Soul-Mate/apps/Launcher.tsx 文件会报错 :261 直接用index作为key

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.npm-cache
dist
dist-ssr
*.local

# build artifact — regenerated by `npm run build:instant-worker`;
# committed copy lives at worker/instant-push/worker.bundle.js
public/instant-worker.bundle.js

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# xiaohongshu-skills (cloned dependency)
xiaohongshu-skills/

# git worktrees
.worktrees/
